# Internal Pages

These are web-based product surfaces that can still be built with HTML/CSS/JS or React after migration.

Examples:

- start page
- settings
- history
- downloads
- Nexa AI sidebar/page

These pages should avoid direct ownership of browser primitives.

They should consume contracts exposed by the shell and browser services.
